name: sim_engine
description: Motor de simulacion de partidos de Manager NBA. Paquete Dart puro, sin dependencia de Flutter, para poder testearse de forma aislada.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
